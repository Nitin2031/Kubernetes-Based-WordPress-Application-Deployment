apiVersion: v1
kind: ResourceQuota
metadata:
  name: wp-resourcequota
  namespace: wp-production
spec:
  hard:
    requests.cpu: "2"
    requests.memory: "2Gi"
    limits.cpu: "4"
    limits.memory: "4Gi"
    pods: "10"
